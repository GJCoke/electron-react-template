import IconButton from "./IconButton.tsx"
import React from "react"

const Push: React.FC = () => {
  return (
    <div>
      <IconButton icon="line-md:plus" />
    </div>
  )
}

export default Push

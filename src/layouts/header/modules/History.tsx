import IconButton from "./IconButton"
import React from "react"

const History: React.FC = () => {
  return (
    <div>
      <IconButton icon="line-md:calendar" />
    </div>
  )
}

export default History
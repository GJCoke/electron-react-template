import React from "react"
import IconButton from "./IconButton"

const Help: React.FC = () => {
  return (
    <div>
      <IconButton icon="line-md:question-circle" />
    </div>
  )
}

export default Help
